# Verixa specifications

Reading order: `01-PRODUCT-SCOPE.md` → `12-CLAUDE-CODE-RULES.md`, then `CLAUDE-MASTER-PROMPT.md`.
Implementation tasks live in `../TASKS/`.

## Progress log

| Task | Status | Notes |
| ---- | ------ | ----- |
| T001 Bootstrap | done | Monorepo, FastAPI `/api/v1/health`, Next.js home page with API status, lint/type/test/build tooling, GitHub Actions CI |
| T002 Dev environment | done | Env-driven config; SQLite + local-dir storage by default (no Docker); PostgreSQL/S3 opt-in; `.env.example`; portability rules in `infra/README.md` |
| T003 Repository structure | done | Package responsibility docstrings; layering rules enforced by `tests/test_architecture.py`; web structure documented in `CLAUDE.md` |
| T004 CI | done | Secret scan (gitleaks) + API + web jobs; Dependabot; `scripts/check.{sh,ps1}` mirror CI locally |
| T005 Database | done | Async SQLAlchemy 2 engine/session; models users, analyses, analysis_files, evidence, provider_calls; Alembic (SQLite batch mode, PG JSONB variant); `/health` reports DB readiness |
| T006 Authentication | done | Argon2id passwords; JWT access + rotating hashed refresh tokens (`user_sessions`); `/auth/*` routes; `CurrentUser` dependency; ownership helper; error envelope + request ids |
| T007 Storage | done | `ObjectStorage` interface; local filesystem adapter with HMAC-signed download route; S3 adapter (boto3, optional extra); key validation + layout helpers |
| T008 Analysis CRUD | done | `AnalysisService` (create, owner-scoped get/list/counts, guarded status transitions, soft delete + storage cleanup); `GET/DELETE /analysis`, `/analysis/counts`; enums moved to `app/enums.py` |
| T009 Dashboard | done | shadcn/ui; register/login/logout via server actions + httpOnly cookies; `proxy.ts` token refresh + route guarding; dashboard with counts, recent analyses, loading/empty/error states (API-down state verified) |
| T010 Image upload | done | `POST /analysis/image`: chunked size cap, magic-byte + Pillow format check, header dimension cap, verify + decode, SHA-256, private storage, no row on rejection; web: drag-and-drop upload form with preview/details, analysis detail page. **Phase 1 complete.** |
| T011 Image validation | done | Validation hardened (frame count recorded; Pillow bomb guard mapped) and formalised as the `validate` pipeline step; step framework (`PipelineRunner`, `AnalysisStep` table + migration, `Dispatcher`); detail page shows real step status |
| T012 Hashing | done | `services/image/hashing.py`: SHA-256, MD5 (compat), aHash/dHash/pHash (numpy DCT, imagehash-compatible), Hamming distance; committed fixtures + golden-value tests; `hashing` pipeline step records values in step details |
| T013 Metadata | done | ExifTool adapter (stdin, fixed args, timeouts, size caps) + Pillow fallback behind `MetadataExtractor`; normaliser with tz-aware timestamp parsing; `image_metadata` table + migration; `metadata` step; `GET /analysis/{id}/metadata`; metadata card with limitations on the detail page |
| T014 C2PA | done | c2patool 0.9.12 adapter (private temp file, fixed args, timeout, size cap) + Null inspector; normaliser (presence/validity/signer/actions/authors/validation codes); `image_provenance` table + migration; `provenance` step; `GET /analysis/{id}/provenance`; provenance card with evidence-level wording |
| T015 Fingerprints | done | `image_fingerprints` table + migration; `hashing` step persists idempotently; `services/image/similarity.py` exact/near comparison; `GET /analysis/{id}/fingerprints` with owner-scoped duplicates; fingerprints card |
| T016 Image report UI | done | Tabbed report (Overview / Metadata / Provenance / AI / Forensics / Matches / Timeline) with URL-addressable, link-based tabs; header evidence summary; Fact → Signal → Interpretation → Unknown overview built from preliminary deterministic evidence (`lib/evidence.ts`, per docs/07 initial rules); evidence cards with level, source, limitation; raw JSON viewers; honest "not available" states. **Phase 2 complete.** |
| T017 Text analysis | done | `POST /analysis/text` (verbatim original in private storage), text pipeline (`normalize`, `language`, `statistics`), `text_analysis` table + migration, `GET /analysis/{id}/text`; paste-text form (mode switch on New analysis), text stats card and text evidence on the report |
| T018 Text fingerprints | done | `services/text/fingerprints.py` (exact/normalised/canonical SHA-256, deterministic MinHash, Jaccard estimate); `text_fingerprints` table + migration; `fingerprints` text step; `/fingerprints` type-aware with owner-scoped exact/normalised/canonical/near matches; text fingerprints card on the Matches tab |
| T019 AI detector interface | done | `providers/ai`: `AIDetector` protocol, `DetectionResult`, threshold→label mapping, `MockAIDetector`, `DetectionCache` + in-memory LRU + `CachedAIDetector`; `ai_detections` table + migration; shared `ai` step (skipped when no provider); `GET /analysis/{id}/ai`; AI tab card with score meter, thresholds and limitations |
| T020 Source search interface | done | `providers/search`: `ImageSourceSearch` / `TextSourceSearch` protocols, `SourceMatch`/`SearchResult`, mock adapters; deterministic distinctive-phrase selection; `reverse_matches` + `source_search_runs` tables + migration; shared `search` step (skipped when none); `GET /analysis/{id}/matches`; source matches card and evidence on the Matches tab |
| T021 Provider call logging | done | `ProviderCallRecorder` (`track()` context manager: status/latency/model/request-hash/cost/bounded response or error), `ProviderCallRepository`, `cached` status, cost fields on results; AI, search, metadata and provenance steps audited; `GET /analysis/{id}/provider-calls`; provider-calls card on the report overview |
| T022 Caching | done | `provider_cache` table + migration; `providers/cache.py` (`ProviderResultCache`, `DbProviderCache` with TTL/hit counts/purge, in-memory variant); AI and search results serialised and served from the persistent cache (shared across users, identity-free); audit trail records `cached`. **Phase 3 complete.** |
| T023 ELA | done | `image_forensics` table + migration (one JSON column per method, artifacts list); `services/image/ela.py` (resave error map, block outliers, connected regions, amplified PNG); `ela` pipeline step (JPEG only, non-critical, skipped-as-not-applicable otherwise); `GET /analysis/{id}/forensics` with signed artifact URLs; Forensics tab with ELA card; evidence rule: single ELA anomaly = POSSIBLE. |
| T024 Compression | done | `services/image/compression.py` (quantisation-table quality estimate vs IJG tables, subsampling, progressive, JFIF/Adobe; 8×8 block-grid phase profile with aligned/offset detection); `compression` step writes `compression_json`; `GET /analysis/{id}/forensics` returns `compression`; Compression card + evidence rules (offset grid / prior JPEG grid = POSSIBLE, encoding facts = UNKNOWN context). |
| T025 Resampling | done | `services/image/resampling.py` (fixed 3×3 predictor residual → p-map → Hann-windowed FFT averaged over tiles; local-median peak ratio; DC/Nyquist/JPEG-1/8 masks); `resampling` step writes `resampling_json`; API + Resampling card (peak list, limitations); evidence: detected trace = POSSIBLE with explicit "resizing is routine" limitation. |
| T026 Noise | done | `services/image/noise.py` (float high-pass residual → per-block MAD sigma; noise-blind texture gate; IQR outliers with floors; connected regions via shared `regions.py`, also used by ELA now); `noise` step writes `noise_json` + `noise.png` artifact; API + Noise card (map, regions, limitations); evidence: localised noise difference = POSSIBLE, consistent = UNKNOWN. |
| T027 Copy-move | done | `services/image/copy_move.py` (stride-1 block features via integral images, packed-key sort, near-neighbour pairs, displacement histogram with coherence/density check, mask artifact); `copy_move` step writes `copy_move_json` + `copy_move.png`; API + Copy-move card; evidence: translated duplicate = POSSIBLE with "repeated content" limitation. All five MVP forensic heuristics now run. |
| T028 Forensic UI | done | `GET /analysis/{id}/file` (signed link to the original, owner only); `ForensicsSummary` (per-method outcome table, correlation note); `ForensicViewer` client component (original with SVG region overlays for ELA/noise/copy-move, map blending with opacity, per-layer toggles, keyboard-accessible); preliminary docs/07 rule: ≥2 independent anomaly families → STRONG. Phase 4 complete. |
| T029 Evidence | done | `services/evidence/engine.py` (pure rules over persisted rows; docs/07 levels; `EvidenceThresholds` from settings; forensic families for independence; explicit conflict records; refs to steps/provider calls/rows); `evidence` step last in both pipelines writes the `evidence` table; `GET /analysis/{id}/evidence`; AI level now requires calibration for PROBABLE (API + engine agree); web uses engine records with a Conflicts group and falls back to the preliminary derivation only while processing. |
| T030 Confidence | done | Configurable level→confidence map, per-rule level overrides clamped to docs/07 ceilings (`LEVEL_CEILING`), synthesis confidence with a per-conflict penalty; thresholds snapshot in step details and in `GET /analysis/{id}/evidence`; settings validation; boundary/positive/negative/conflict unit tests (`tests/test_evidence_thresholds.py`). |
| T031 Conflicts | done | Conflict rules: provenance vs STRONG/PROBABLE signal (T029), capture-after-signing, published-before-capture (`utils/timeparse.py`); conflict records keep both sides, reference both, lower synthesis confidence; report alert + Conflicts group; tests for positive/negative/tolerance/naive cases. |
| T032 Timeline | done | `timeline_events` table + migration; `services/evidence/timeline.py` (events from provenance signing/actions, metadata capture/modification, source publication/discovery, submission; certainty from the evidence level; raw time + tz flag kept; undated last); written by the evidence step with `source_evidence_ids`; `GET /analysis/{id}/timeline`; Timeline tab (vertical, level + source + raw time) for image and text. |
| T033 LLM synthesis | done | `providers/llm/` (interface, mock, OpenAI JSON-schema adapter, cache); `services/synthesis/` (request builder over stored rows with evidence fingerprint; grounding checks: invalid citations dropped, ungrounded sections flagged, level/certainty warnings); `syntheses` table + migration; `synthesis` step last in both pipelines (audited provider call, cached); `GET /analysis/{id}/synthesis` with `current` staleness flag; Interpretation section on the overview with citations. |
| T034 Overview | done | `services/reports/overview.py` (groups by level/kind, synthesis confidence, steps, engines from the audit trail, thresholds, level definitions, methodology notes); `GET /analysis/{id}/overview` embedding evidence records and the synthesis; Overview tab regrouped (verified / strong / probabilistic / conflicts / interpretation / unknown) + Methodology card. |
| T035 Matches | done | `services/search/summary.py` (domains, kinds, similarity range, dated count, earliest reported date as reported) added to the matches response; Matches tab rebuilt as a client card: per-match URL/title/similarity bar/provider/kind/reported date/discovery time/matched phrase/snippet/raw record, POSSIBLE badge, sort + kind/domain filters, limitations incl. provider-scale and third-party-link notes. |
| T036 PDF | done | `reports` table + migration; `services/reports/bundle.py` + `pdf.py` (ReportLab; all sections incl. forensic maps, thumbnail, footer disclaimer) + `service.py` (ownership, completed-only, private storage, sha/page count, failure recorded); `POST /analysis/{id}/report`, `GET /analysis/{id}/reports`, `GET /reports/{id}`, `GET /reports/{id}/pdf` (signed link); delete sweeps report files; Export card with server action. |
| T037 Retention | done | Settings for raw-content hours, provider-response days, deleted-record grace, record retention, sweep interval; `analyses.kept_at/content_purged_at`, `provider_calls.purged_at` (+ migration); `services/retention.py` passes + `workers/retention.py` (lifespan task, CLI); keep/release endpoints; purged content hidden from file/forensics/report links; report rows marked expired; web retention notice with Keep/Release. |
| T038 Usage | done | `usage` table + migration (monthly counters: analyses/image/text/reports/provider calls/cost/bytes); `services/usage.py` (period, snapshot with computed current storage, limits → 429, provider budget); hooks in analysis creation, provider-call recorder, report and forensic-artifact storage; paid steps skip when the budget is spent; `GET /usage`, `GET /usage/history`; dashboard Usage card with meters. |
| GPS coordinates | done | Metadata normaliser records `gps_latitude`/`gps_longitude` (decimal degrees from ExifTool composites or EXIF DMS + refs), `gps_altitude_m` and the receiver's UTC `gps_time`; range/partial checks add warnings; `metadata.gps` evidence keeps the numbers in `data` (claim never quotes them); `metadata.gps_time` timeline event; Metadata card shows the position with an OpenStreetMap link; PDF lists position and GPS time. |
| Forensics: thumbnail + ghosts | done | `services/image/thumbnail.py` (own EXIF IFD1 parser; correlation, flip/rotate transforms, aspect check, block-wise difference regions with correlation outside regions; embedded thumbnail + difference map artifacts) and `services/image/double_compression.py` (JPEG ghost curve over q50-100 per 16 px block with per-block normalisation, lowest strong dip = earlier quality, localised regions, centre-crop cap, DC-histogram periodicity corroboration; ghost map artifact); `thumbnail_json`/`double_compression_json` columns + migration; steps in the image pipeline; evidence rules `forensics.thumbnail.{mismatch,region,geometry,consistent,na}` (own family) and `forensics.double-compression.{localized,global,none,na}` (localised shares the ELA family; global is history); cards, summary rows, viewer maps/regions, PDF. |
| Lineage | done | `services/image/lineage.py`: generator markers from PNG text chunks (A1111 `parameters`, ComfyUI `prompt`/`workflow`, InvokeAI, NovelAI, generic payload tokens) and from EXIF/XMP software, creator, description strings (name table); IPTC `DigitalSourceType` (algorithmic types flagged); XMP `DocumentID`/`InstanceID`/`OriginalDocumentID`/`DerivedFrom` and `xmpMM:History` actions with software agent and time; Pillow extractor now reads PNG chunks and its nested XMP is flattened; evidence rules `metadata.generator` (STRONG ceiling), `metadata.source-type` (STRONG when algorithmic, else POSSIBLE), `metadata.edit-history` (STRONG); `metadata.edit` timeline events; Metadata card 'Declared lineage' section; PDF rows. |
| T047 C2PA phase 4 | done | Signer trust evaluation: the official C2PA trust list is bundled (`app/providers/provenance/trust/C2PA-TRUST-LIST.pem` + meta with source URL, fetch time, sha256) and refreshed by `scripts/refresh_trust_list.py` (through `assert_outbound_allowed`, host passed explicitly); settings `VERIXA_C2PA_TRUST_MODE` (bundled / custom / off) and local-file-only `VERIXA_C2PA_{TRUST_ANCHORS,ALLOWED_LIST,TRUST_CONFIG}_PATH` (URLs refused, files must exist); the adapter runs a second, separate `-d trust …` pass with paths relative to the trust directory (the engine parses those options as URL-or-relative-path); `trust` view (evaluated, mode, list version, engine state, trust codes, verdict) with a conservative verdict (explicit trusted / untrusted signals only); columns `image_provenance.trusted` + `trust_list_version` (migration `a8100edd051e`); rules `provenance.trusted` (VERIFIED fact) and `provenance.untrusted-signer` (UNKNOWN); `provenance.valid` limitation now points at the trust record; `/health` engines report trust mode and list version; Provenance card "Signer trust" section and badge text; PDF rows; real-binary tests for bundled (unlisted) and custom (trusted) anchors; release checklist item. |
| T046 C2PA phase 3 | done | c2patool pinned to v0.28.0 (c2pa-rs 0.91) in CI (api + e2e jobs) and the API image (release asset renamed `v<ver>-x86_64-unknown-linux-gnu.tar.gz`); adapter reads the 0.28 shapes (validation results/state in the summary, `c2pa.actions.v2`, v2 `softwareAgent` objects, `common_name`) while still accepting 0.9.x; the engine's own network access is switched off through a shipped settings file (`providers/provenance/c2pa_settings.toml`: no remote-manifest fetch, no OCSP) passed as `--settings` when the binary supports it, and `asset_args()` never carries a URL; `VERIXA_C2PA_REMOTE_MANIFEST_FETCH` (default false) refused in production by preflight; 0.28 files an unlisted signer under *failure*, so `validation.active_manifest` now has `trust` and `identity` families and `failure` keeps integrity only; `manifest_location` from `--info`; XMP `dcterms:provenance` recorded as `provenance_url` (lineage, metadata schema) and surfaced by rule `provenance.remote` (UNKNOWN, host only, never fetched) with a security test that blocks sockets during the analysis; recorded 0.28 JSON fixtures incl. the Trusted state; Windows dev install folder `%LOCALAPPDATA%\Programs\c2patool-0.28.0`. |
| T045 C2PA phase 2 | done | Assertion depth and ingredient tree from the manifest store (`services/image/provenance_depth.py`): hash coverage (`c2pa.hash.data` exclusions from the detailed store), signed digital source types from `c2pa.actions(.v2)` (shared IPTC helper with lineage), training/mining permissions, CAWG identity presence, software agents (string or object), recursive ingredient tree with per-ingredient validation and failure codes, manifest chain with signing-order check; `--tree` captured and served (`tree`); rules `provenance.source-type` / `provenance.training-mining` / `provenance.identity` (STRONG ceiling, POSSIBLE when unverified) and `provenance.ingredient.invalid` (POSSIBLE), `provenance.valid` data gains hash coverage and agents; conflicts `conflict.source-type` and `conflict.manifest-order`; timeline `provenance.ingredient-signed`; Provenance card sections "Signed declarations" and "Ingredients and manifest chain"; PDF rows; Playwright spec on the signed fixture (CI e2e job now installs c2patool). |
| T044 C2PA phase 1 | done | Provenance foundation: c2patool capability probe (`--version`/`--help` once per process, feature switches by flag presence), one retry only for process-start/signal failures, `--info` facts, both output shapes parsed (`validation_status` and `validation_results`/`validation_state`) into a structured `validation` view; `classify_code()` separates integrity, trust, identity and info codes so an untrusted signer never reads as an altered manifest; `/health` reports engine status and version; PDF prints the provenance limitations; evidence cards render each record's `data` generically (scalars as rows, nested values collapsed); recorded 0.9.12 JSON fixtures. |
| T043 Beta acceptance | done | `docs/13-RELEASE-CHECKLIST.md`: release checklist (code/tests, security and privacy, core flows, report quality, failure handling, retention, deployment, docs) with the dated first acceptance run: full local + CI checks, 60 scripted live checks against the API, retention sweep on a scratch instance, dependency audits; defects fixed: blank `VERIXA_SECRET_KEY=` broke login (dev/test now fall back to the built-in default, production refuses), E2E API gets its own key, web image needed `public/`. |
| T042 Deploy | done | `services/api/Dockerfile` (Python 3.12, ExifTool, pinned c2patool, `.[postgres,s3]`, non-root, liveness healthcheck) and `apps/web/Dockerfile` (Next standalone output gated by `NEXT_OUTPUT`, built from the repo root); `infra/compose.yml` (PostgreSQL 16, one-shot `migrate` = preflight + `alembic upgrade head`, API, web, Caddy with automatic TLS, optional MinIO profile), `infra/Caddyfile`, `infra/.env.example`; `Settings.production_problems()` + `python -m app.preflight` refuse non-deployable production settings (debug, SQLite, local storage, non-https origins, metrics without token); web `API_BASE_URL` server-side override; CI `images` job builds both images and smoke-tests preflight, liveness and the landing page; `infra/README.md` production section. |
| T041 Observability | done | Structured logging with `request_id`/`analysis_id` context (`utils/observability.py`, text or JSON, uvicorn access log replaced by a route-template request log); per-step, per-analysis and per-provider-call logs with durations and failure visibility (warnings, error codes only); in-process Prometheus metrics (`utils/metrics.py`, `GET /metrics`, optional bearer token, refused in production without one); readiness `/health` (database, storage probe, provider names, uptime) + `/health/live`; landing page shows storage/providers; alembic no longer disables existing loggers. |
| T040 E2E | done | Playwright suite in `apps/web/e2e/` (auth, image analysis, text analysis, PDF export, deletion/ownership) with hydration-safe helpers; `playwright.config.ts` boots an isolated API (scratch SQLite, mock providers) and a production web build in `.next-e2e`; delete control on the report page (`delete-analysis-button.tsx`, `deleteAnalysis` action, dashboard notice); text-form number formatting pinned to one locale (hydration mismatch fix); CI `e2e` job. |
| T039 Security | done | Hardening pass: security headers on every API response (`utils/security_headers.py`) and web page (`next.config.ts`); login/registration throttling per email and client address (`utils/ratelimit.py`, 429 + `Retry-After`); outbound URL policy for provider endpoints (`utils/urlpolicy.py`, `VERIXA_OUTBOUND_ALLOWED_HOSTS`) and non-web provider links dropped/never rendered as anchors; filename hygiene for titles and `Content-Disposition` (`utils/filenames.py`); log redaction record factory (`utils/logredact.py`); `tests/test_security.py` covering IDOR across all routes, report access, malicious/oversized/malformed uploads, prompt injection, crashing providers, signed-URL expiry/tamper/key swap, throttling, headers, secrets and redaction. |
